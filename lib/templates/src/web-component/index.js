import { define } from 'nautil/web-component'
import App from '../app/app.jsx'

// `MY_APP_CSS_TEXT` can be changed into any other constants, nautil-cli will discern it automaticly
define('my-app', App, MY_APP_CSS_TEXT)
